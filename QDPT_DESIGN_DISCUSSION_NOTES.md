# QDPT 设计笔记

更新：2026-09-23。本文只保留当前 V0 基准、必要约束和分级候选；已删除过时讨论与重复草案。V0 实现配置现已冻结：`r_q=128`、`r_delta=192`、三块各64、P20＋Visual18，偏移输出层权重 `Normal(0,1e-4)`、零 bias。服务器实验仍未授权。

## 当前目标与边界

- 先验证低参数方案可用且有明确性能收益，再考虑多 seed 稳定性；不以 59 分或追平旧 QDPT/LoRA 作为首轮硬门槛。
- 保留可训练 static LLM Prompt、原生视觉输入与 DeepStack；不修改 LLM 权重或内部 attention。static 指跨样本共享，不是训练时冻结。
- 三层采用视觉代码索引 5/11/17；各层问题条件地图读取共同原生 Value，保留三份摘要，再用于问题 token 偏移。
- 结果事实以 EXPERIMENT_RESULTS.md 和 result.md 为准，排期以 plan.md 为准。此处候选库不是批量实验清单。

## 必要实现与解释约束

- ADePT 原文第 5 页 §3.3 式(3)为 token-shared 窄 MLP；图像条件是本项目扩展。来源：桌面 ICLR-2025-adept-adaptive-decomposed-prompt-tuning-for-parameter-efficient-fine-tuning-Paper-Conference.pdf。
- 问题池化和偏移只覆盖真实问题，不读取答案、系统模板、图像标记或 padding。只用原问题生成条件，避免递归依赖；训练和推理掩码一致，生成答案不套问题偏移。
- 冻结视觉骨干不等于整个视觉前向 no_grad；保留视觉 Prompt 时，其相关路径必须传回梯度。后段 Prompt 不能改变同次前向已产生的浅层特征。
- 零输出初始化可能暂时阻断上游首步梯度；避免多重零投影或零 gate 令分支永久失活。检查各分支实际梯度、偏移尺度和图像条件利用，不只看 loss。
- 问题偏移会影响 Q/K/V 与后续隐状态，不能等同于精确区域加权；地图可视化不是因果证明，单 seed 也不能证明稳定性。

## V0 公式基准与分级候选库（2026-09-23）

用户要求以此前完整公式版为基准记录，并对讨论过的变体分级。目标是先证明小参数方案可用且有明确收益，再验证稳定性。以下等级表示决策优先级，不是必须完成的实验矩阵；不同时叠加多个候选。完整结果只写入两份实验账本，排期只在 plan.md 决定。

### V0：问题引导的三层选择、共同 Value、分层条件问题偏移

符号：E_q=[e_1,...,e_L] 为真实问题 embedding，形状 L*d；d=2560；H_l 为视觉索引 l∈{5,11,17} 的特征，形状 N_l*d_v，d_v=1024；V=[v_1,...,v_M] 为最终原生 post-merger 视觉表示，形状 M*d；r_q 为问题/打分宽度；r_delta 为偏移瓶颈宽度。两种宽度不要求相等。

1. 问题低维上下文与池化：

   X = E_q W_t

   X_tilde = X + phi(Conv1D(X))

   alpha_i = softmax_i(w_p^T Norm(X_tilde_i))

   u = sum_i alpha_i X_tilde_i

   W_t 为 d*r_q，w_p 为 r_q 维可学习向量；Conv1D 沿问题序列提取局部上下文，phi 为非线性。仅真实问题参与，卷积 padding 与掩码一致；不读取答案，不额外执行 LLM。

2. 三层独立轻量单头打分：

   q_l = u W_Q_l + b_Q_l

   k_lj = Norm(h_lj) W_K_l

   a_lj = softmax_j(q_l^T k_lj / sqrt(r_q))

   W_Q_l 为 r_q*r_q，W_K_l 为 d_v*r_q。每层一张问题条件地图。没有新建 Value/CA 输出投影，不复用冻结 Q/K 作为 V0 默认。

3. 地图空间对应与共同内容读取：

   a_bar_l = G_l a_l

   z_l = sum_j a_bar_lj v_j

   G_l 为 M*N_l 的固定空间归属算子，将细粒度概率质量合并到最终视觉 token 网格；按真实 token 重排/合并定义，实际实现不必存稠密矩阵。排除视觉 Prompt、padding 和其他图像；保持非负与概率和。每层读取同一份 V，得到三个 d 维摘要，不提前平均。

4. 问题条件层权重与保留来源的瓶颈：

   beta = softmax(u W_beta + b_beta)

   c_l = Norm(z_l) C_l

   c = concat(beta_5 c_5, beta_11 c_11, beta_17 c_17)

   W_beta 为 r_q*3；C_l 为 d*r_l，sum_l r_l=r_delta。层权重不强制相等、不预设深层必胜。三个分块先保留来源，随后由共享输出头组合。sum_l d*r_l=d*r_delta，独立分块投影的矩阵预算没有变为三倍。

5. token-shared 条件偏移：

   delta_i = ReLU(e_i W_down + c + b_1) W_up + b_2

   e_i_prime = e_i + delta_i

   W_down 为 d*r_delta，W_up 为 r_delta*d。真实问题 token 共用一个网络；静态 Prompt、原生视觉输入、chat 模板和原生 DeepStack 保留。概念输入为 [P_static; V_native; E_question/chat_prime]，不代表重写模板位置或删除图像标记。

容量边界：共同 Value 不能恢复最终表示已丢失的中间层内容；三份摘要不等于完整区域关系；分块投影仍有压缩，最终偏移仍受 r_delta 维输出子空间约束。该基准避免了直接相加异构层内容，不承诺定位或稳定性已解决。

参数预算：仅上述主要矩阵约为 d*r_q + 3*r_q^2 + 3*d_v*r_q + 3*r_q + 3*d*r_delta，另加卷积、bias、归一化、静态/视觉 Prompt。V0 参数须在实现后完整核算。

待定运行细节：N_static（建议先 P20）、视觉适配（建议先保留 Visual18，但非用户已冻结选择）、H_l 精确读取点、r_q/r_delta/r_l、卷积类型/核宽、归一化与初始化、参数组 LR、固定训练 seed 与止损线。不得由工程代理把候选库全部自动叠加。记录不同分支梯度、图像条件利用、参数及吞吐；不把热力图当因果证明。

### 等级 A：V0 有结果后优先选择的必要性验证

不要求首轮同时完成；先单次 V0，再按现象选择一项。比较应保留其他配置和训练协议，参数/初始化变化如实记录。

| ID | 相对 V0 的单一改变 | 潜在价值 | 风险/解释边界 | 建议 |
|---|---|---|---|---|
| A1 | c=0，重新训练文本偏移 MLP，其余背景相同 | 区分视觉条件收益与普通 ADePT 收益 | 总功能容量减少；推理置零不能代替重训 | 优先建议，V0 有信号后做 |
| A2 | 各层地图改为均匀权重，保留层权重与偏移网络 | 判断局部选择是否胜过全局图像条件 | 小区域证据被平均；Q/K 失去作用，须报告有效参数 | 建议作为高信息量对照 |
| A3 | 每层 q_l 改为静态可学习查询，beta(u)与偏移仍保留 | 隔离地图中的问题引导是否必要 | 仍有其他问题条件路径；参数变化，不是全模型去问题条件 | 建议，优先于复杂扩容 |
| A4 | 去掉问题池化前 Conv1D 残差 | 判断局部上下文的实际价值 | 退化为对原始低维 embedding 的加权词袋 | 可选，成本受控时做 |
| A5 | 仅保留 17 层读取及对应摘要 | 判断三层是否真的提供增益 | 分支与瓶颈分配改变；必须标明自然缩减或容量匹配协议 | 可选，不展开层扫描 |

### 等级 B：轻量改进或输入接口退路，按诊断触发

| ID | 改变 | 潜在价值 | 风险/解释边界 | 建议 |
|---|---|---|---|---|
| B1 | 问题条件 beta 换成三个全局可学习层权重 | 降低层路由自由度，保留不等权 | 不同问题可能需要不同深度；不保证改善稳定 | 条件建议：beta 波动或塌缩时 |
| B2 | 混合三张地图后只读一次 V、只保留一份摘要 | 更简单聚合，减少部分计算 | 丢掉层来源；不保证显著减少参数，须匹配投影预算 | 条件建议：三份摘要高度冗余时 |
| B3 | 数据相关/语义相关的查询或静态 Prompt 初始化 | 减少随机起点偏离有效表征的机会 | 文本与视觉空间不可直接互换；共享固定初始化不等于消除训练随机性 | 值得保留，先明确初始化对象与空间 |
| B4 | 仅改一个瓶颈宽度 r_q 或 r_delta | 区分定位容量与偏移表达容量不足 | 参数增长、过拟合；一次一个宽度、不做网格扫描 | 条件建议：有容量不足证据时 |
| B5 | 相同三层证据改接三个额外 soft prompt，取消图像条件问题改写 | 保留原问题语义，验证写入接口 | 需校准视觉摘要到输入空间；序列长度与参数不同 | 建议作为主要接口退路，不与 V0 叠加 |
| B6 | 新建打分器换为原生 ViT 冻结 Q/K 复用 | 借用预训练视觉匹配结构 | 文本 Query 对齐、归一化/RoPE/多头复杂，未必更省算力 | 可选消融，已入 plan，非首轮 |

### 等级 C：可能提高上限，但改动或解释成本较高

| ID | 改变 | 潜在价值 | 风险/解释边界 | 建议 |
|---|---|---|---|---|
| C1 | 共同 V 改为三层各自的 Value | 补入最终视觉表示可能丢失的细节 | 重新承担跨层语义对齐，可能恢复重型融合 | 条件建议：共同内容确有不足再做 |
| C2 | 单层单查询扩为少量读取槽 | 保留多个区域、比较或空间关系 | 槽重复，摘要/瓶颈预算变化，选择不稳定 | 条件建议：多区域问题为明确短板时 |
| C3 | Visual18 替换为自然层16～24每层5个独立 Prompt | 后段持续轻量视觉适配 | 同时改变数量/层位/参数化；无法增强已产生的5/11特征 | 待 V0 有效后可选，先替换不额外堆叠 |
| C4 | 在代码层5/11/17前各放少量视觉 Prompt，读对应输出 | 让各出口可适配 | 早层修改传到后层，三路不独立；反传成本更高 | 暂不优先，与C3互为替代 |
| C5 | 用共享瓶颈残差 h'=h+f(h) 替代视觉 Prompt | 无额外 token，直接强化领域视觉表达 | 本质接近 Adapter；扰动真实视觉内容；注入层位影响成本 | 有潜力，V0 成立后独立比较 |
| C6 | 视觉残差进一步条件化为 h'=h+f(h,u) | 图像按问题呈现不同特征 | 两端共同改写、捷径与共适应风险增加 | 暂不建议，待C5证实有用 |

### 等级 D：仅归档，不进入当前候选执行序列

- D1 晚启动动态：旧固定10%方案已有负结果；仅作为新结构未来时序退路，不复跑旧方案、不扫描比例（潜在价值：改变早期耦合；风险：缩短动态有效训练且压低高分 seed；当前不建议）。
- D2 预训练并永久冻结 P20：旧方案已失败且增加训练成本（潜在价值：固定静态参照；风险：接口不兼容、性能回退；当前不建议）。
- D3 硬 Top-k、稀疏路由、额外对比/正交损失、动态位置/层搜索：先前作为思路出现，但无针对当前结构的证据（潜在价值：选择更集中或槽分工；风险：离散切换、目标冲突、调参膨胀；当前不建议）。
- D4 直接修改 LLM attention logits 或内部 K/V：不符合用户当前的输入侧适配边界，不作为本轮实验。

### 使用顺序

V0 最少运行细节已冻结，下一步只做授权后的单次 seed44 Validation 可用性验证；有效时优先从 A1/A2/A3 中选能回答当前疑问的一项，不同时启动整套消融。明确瓶颈后才选 B/C 对应变体。数据诊断或推理级干预不能冒充重训消融；后续多 seed 稳定性单独验证，不以单 seed 宣称稳定。没有新服务器运行授权。
