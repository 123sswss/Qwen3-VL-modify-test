# 我看到的“不是 bug，但值得知道”的两个设计点

## 设计点 1：多图样本现在是简单平均

你现在对同一个 sample 的多个图像：

- `alpha` 是 mean
- `visual_pooled` 也是 mean

这当然能用，但它的含义是：

> “一个样本里多张图，被压成一个平均视觉摘要。”

### 可能的副作用

如果一个样本里多张图差异很大：

- 有一张很关键
- 另一张是噪声图

那平均会把关键信号冲淡。

### 但我不认为现在要改

对你这轮来说，这不是 bug，属于一阶段可优化项__。\
先别动，保持小改动更重要。
